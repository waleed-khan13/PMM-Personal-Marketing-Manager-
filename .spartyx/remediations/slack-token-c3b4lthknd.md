# Spartyx Remediation Plan

This remediation artifact was generated from a saved Spartyx scan. It is intentionally review-first: apply the patch in application code, run the verification gate, then mark the finding fixed.

## Finding
- Title: Slack Token
- Severity: Critical
- Category: Secrets Exposure
- Rule: AXM-SEC-SLACK-TOKEN
- Location: `backend/tests/test_local_core.py`

## Assessment
Credential material appears to be hardcoded in source-controlled code.

## Recommended Strategy
Revoke and rotate the exposed value, move it to the approved secret manager or deployment environment, and remove it from source history where practical.

## Ownership
- Owner: Platform owner
- Risk: High
- Affected files: `backend/tests/test_local_core.py`

## Fix Steps
1. Revoke and rotate the exposed credential.
2. Move the secret into the approved secret manager or deployment environment.
3. Replace source usage with environment/secret-manager lookup.

## Verification Gate
1. Rotate the exposed secret before deploy.
2. Confirm the value is absent from source and build artifacts.
3. Run the affected test suite.
4. Re-run the Spartyx scan and confirm the finding moves to fixed or guarded.

## Patch Preview
Preview replaces hardcoded secret with environment/secret-manager lookup. Rotate the real secret separately.

## Code Patch Engine
Applied secret_externalization to `backend/tests/test_local_core.py`.
Replaced hardcoded secret-like value with os.getenv("API_KEY", "").
```diff
--- backend/tests/test_local_core.py
+++ backend/tests/test_local_core.py
- api_key = "[REDACTED]"
+ api_key = os.getenv("API_KEY", "")
```
> Automated patch - verify correctness before applying to production.

- Automated patch - verify correctness before applying to production.
- Set API_KEY in the deployment secret manager before release.
- Rotate the exposed credential before merging this branch.

### backend/tests/test_local_core.py
Externalize secret lookup.
```diff
- bot_token = "[REDACTED]"
+ bot_token = os.getenv("S_T")
```
## Planner Warnings
- Diff is redacted and not auto-applied. Rotate exposed credentials before deployment.

## References
- No external references were provided by the scanner.

Generated by Spartyx remediation workflow.
